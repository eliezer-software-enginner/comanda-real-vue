# comanda-real-vue

Criei um script para subir o emulador, popular os dados e subir a aplicação

- só rodar

```sh
.\scripts\inicializa-app.ps1
```

- ou rodar:

- firebase emulators:start

- npm run seed

- npm run dev

./scripts/inicializa-app.sh

CTRL + P para pesquisar por arquivos
CTRL + P + @ para pesquisar por códigos especicos no arquivo atual

---

comando para realizar build e testes de integração antes do commit
npm run pre-commit
